{
  "name": "json-xml-mediator",
  "displayName": "JSON XML Mediator",
  "version": "0.1",
  "provider": "WSO2",
  "categories": [
    "Transformation"
  ],
  "description": "Mediates payload format across backend boundaries by converting requests from the downstream payload format to the upstream payload format and converting responses back for downstream clients."
}
